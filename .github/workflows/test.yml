name: Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Restore packages
        run: npm install

      - name: Compile, lint and test (Linux)
        run: xvfb-run -a npm test
        if: runner.os == 'Linux'

      - name: Compile, lint and test (Windows, Mac)
        run: npm test
        if: runner.os != 'Linux'
